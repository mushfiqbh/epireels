# `@epireels/types`

Shared TypeScript types used by `apps/web`, `apps/api`, and `apps/mobile`.

- Dependency-free (no runtime dependencies).
- Compiles to ESM with declaration maps.
- Imported as `@epireels/types` from any app.
